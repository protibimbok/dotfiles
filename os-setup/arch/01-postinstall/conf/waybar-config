{
    // General Settings (based on ml4w-modern theme)
    "layer": "top",
    "margin-top": 0,
    "margin-bottom": 0,
    "margin-left": 0,
    "margin-right": 0,
    "spacing": 0,

    // Modules Left
    "modules-left": [
        "hyprland/workspaces"
    ],

    // Modules Center
    "modules-center": [
        "clock"
    ],

    // Modules Right
    "modules-right": [
        "pulseaudio",
        "backlight",
        "bluetooth",
        "network",
        "battery",
        "cpu",
        "memory",
        "tray",
        "custom/power"
    ],

    // Workspaces - shows only current workspace number
    "hyprland/workspaces": {
        "format": "{id}",
        "on-scroll-up": "hyprctl dispatch workspace r-1",
        "on-scroll-down": "hyprctl dispatch workspace r+1",
        "on-click": "activate",
        "active-only": true,
        "all-outputs": true
    },

    // Clock
    "clock": {
        "format": "{:%H:%M %a}",
        "format-alt": "{:%Y-%m-%d}",
        "tooltip-format": "<tt>{calendar}</tt>",
        "calendar": {
            "format": {
                "today": "<span color='#f38ba8'><b>{}</b></span>"
            }
        }
    },

    // CPU
    "cpu": {
        "format": "󰍛 {usage}%",
        "interval": 2,
        "on-click": "kitty --title 'System Monitor' btop"
    },

    // Memory
    "memory": {
        "format": "󰘚 {}%",
        "interval": 2,
        "on-click": "kitty --title 'System Monitor' btop"
    },

    // Backlight/Brightness
    "backlight": {
        "format": "{icon} {percent}%",
        "format-icons": ["󰃞", "󰃟", "󰃠"],
        "scroll-step": 5,
        "on-scroll-up": "brightnessctl -e4 -n2 set 5%+",
        "on-scroll-down": "brightnessctl -e4 -n2 set 5%-"
    },

    // Battery
    "battery": {
        "interval": 10,
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "{icon} {capacity}%",
        "format-charging": "󰂄 {capacity}%",
        "format-plugged": "󰚥 {capacity}%",
        "format-alt": "{icon} {time}",
        "format-icons": ["󰁺", "󰁻", "󰁼", "󰁽", "󰁾", "󰁿", "󰂀", "󰂁", "󰂂", "󰁹"]
    },

    // Pulseaudio/Volume
    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-bluetooth": "{volume}% {icon}",
        "format-bluetooth-muted": "󰝟 {icon}",
        "format-muted": "󰝟",
        "format-icons": {
            "headphone": "󰋋",
            "hands-free": "󰋎",
            "headset": "󰋎",
            "phone": "󰏲",
            "portable": "󰏲",
            "car": "󰄋",
            "default": ["󰕿", "󰖀", "󰕾"]
        },
        "on-click": "pavucontrol"
    },

    // Bluetooth
    "bluetooth": {
        "format": "󰂯 {status}",
        "format-disabled": "",
        "format-off": "",
        "format-connected": "󰂱 {num_connections}",
        "format-no-controller": "",
        "interval": 30,
        "on-click": "blueman-manager",
        "tooltip-format-connected": "Connected: {num_connections}\n\n{device_enumerate}",
        "tooltip-format-enumerate-connected": "{device_alias}",
        "tooltip-format-enumerate-connected-battery": "{device_alias}\t{device_battery_percentage}%"
    },

    // Network
    "network": {
        "format": "{ifname}",
        "format-wifi": "󰤨 {signalStrength}%",
        "format-ethernet": "󰈀",
        "format-disconnected": "󰤮",
        "tooltip-format-wifi": "{essid} ({signalStrength}%)",
        "tooltip-format-ethernet": "Ethernet connected",
        "tooltip-format-disconnected": "Disconnected",
        "on-click": "nm-connection-editor"
    },

    // System Tray (for nm-applet, blueman-applet)
    "tray": {
        "icon-size": 18,
        "spacing": 10
    },

    // Power Button
    "custom/power": {
        "format": "󰐥",
        "tooltip-format": "Power Menu",
        "on-click": "wlogout",
        "on-click-right": "hyprlock"
    }
}
