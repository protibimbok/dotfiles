general {
    no_fade_in = true
    grace = 0
}

background {
    monitor =
    # Use a solid color (Red for testing) instead of screenshot
    color = rgba(255, 0, 0, 1.0)
    
    # IMPORTANT: Set path to empty and blur to 0 for VMs
    path = 
    blur_passes = 0
}

input-field {
    monitor =
    size = 250, 50
    outline_thickness = 2
    dots_size = 0.2
    dots_center = true
    outer_color = rgba(0, 0, 0, 1.0)
    inner_color = rgba(255, 255, 255, 1.0)
    font_color = rgba(0, 0, 0, 1.0)
    fade_on_empty = false
    placeholder_text = <i>Password...</i>
    hide_input = false
    position = 0, 0
    halign = center
    valign = center
}

label {
    monitor =
    text = Test Lock
    color = rgba(255, 255, 255, 1.0)
    font_size = 40
    font_family = Sans
    position = 0, 80
    halign = center
    valign = center
}







