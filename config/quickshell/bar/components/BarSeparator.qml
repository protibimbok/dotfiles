import QtQuick
import qs.theme

Rectangle {
    width: 1
    height: 12
    radius: 0.5
    anchors.verticalCenter: parent ? parent.verticalCenter : undefined
    color: Theme.colors.text
    opacity: 0.12
}
